---
import GpuCalculator from './GpuCalculator';

interface Props {
  componentId: string;
}

const { componentId } = Astro.props;
---

{componentId === 'gpu-calculator' && <GpuCalculator client:load />}
